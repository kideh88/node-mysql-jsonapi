// requires all needed classes and also the main mysql depencncy
// main client initiate will do this.adapter = require('/adapter/DBTYPE.js') thereby automatically have all needed classes
// scanner schema and database all on the same object

// IS THIS JSONAPI MYSQL ADAPTER?
// felixs mysql is node-mysql adapter?

this.headers = {
  'Content-Type': 'application/vnd.api+json',
  'Accept': 'application/vnd.api+json'
}
